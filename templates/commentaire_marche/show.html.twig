{% extends 'base.html.twig' %}

{% block title %}CommentaireMarche{% endblock %}

{% block body %}
    <h1>CommentaireMarche</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ commentaire_marche.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ commentaire_marche.titre }}</td>
            </tr>
            <tr>
                <th>Note</th>
                <td>{{ commentaire_marche.note }}</td>
            </tr>
            <tr>
                <th>Contenu</th>
                <td>{{ commentaire_marche.contenu }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_commentaire_marche_index') }}">back to list</a>

    <a href="{{ path('app_commentaire_marche_edit', {'id': commentaire_marche.id}) }}">edit</a>

    {{ include('commentaire_marche/_delete_form.html.twig') }}
{% endblock %}
